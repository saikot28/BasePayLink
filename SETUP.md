# BasePayLink v2
1. Replace the old project files with this version.
2. Run `npm install`.
3. Run `npm run dev`.
4. Create `.env.local` with `NEXT_PUBLIC_PAYMENT_CONTRACT=0x...` after deploying the contract.
5. Test on Base Sepolia before Base Mainnet.
Native Base USDC: `0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913`
Never share seed phrases/private keys.
